% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCosMxGenes_Fullfill_ZommBound.R
\name{plotCosMxGenes}
\alias{plotCosMxGenes}
\title{Plot CosMx cell polygons and gene transcripts with full + zoom panels}
\usage{
plotCosMxGenes(
  polygons,
  tx,
  genes,
  colors = NULL,
  celltype_df = NULL,
  celltype_col = "cell_type",
  celltype_palette = NULL,
  celltype_alpha_full = 0.35,
  celltype_alpha_zoom = 1,
  show_celltype_legend_full = TRUE,
  show_celltype_legend_zoom = FALSE,
  show_genes_full = FALSE,
  show_genes_zoom = TRUE,
  full_celltype_mode = c("fill", "outline", "both"),
  zoom_celltype_mode = c("outline", "fill", "both"),
  zoom.x = NULL,
  zoom.y = NULL,
  zoom.width.x = 1000,
  zoom.width.y = 1000,
  microns_per_pixel = 0.12028,
  scale_length_um_full = 50,
  scale_length_um_zoom = 25,
  label_size_full = 22,
  label_size_zoom = 25,
  point_size_full = 0.3,
  point_size_zoom = 2,
  point_alpha_full = 1,
  point_alpha_zoom = 1,
  polygon_linewidth_full = 0.3,
  polygon_linewidth_zoom = 1,
  show_zoom_rect = TRUE,
  zoom.rect.color = "white",
  zoom.rect.alpha = 0,
  zoom.rect.linewidth = 1,
  zoom.rect.linetype = "dashed"
)
}
\arguments{
\item{polygons}{A data.frame containing polygon vertices. Must include columns
\code{fov}, \code{x_global_px}, \code{y_global_px}, and \code{cellID} (or \code{cell_ID}).}

\item{tx}{A data.frame of transcript coordinates. Must include \code{target}, \code{x_global_px}, \code{y_global_px}.}

\item{genes}{Character vector of genes to plot.}

\item{colors}{Colors for each gene in \code{genes}.}

\item{celltype_df}{Optional. data.frame mapping cells to types. Must include \code{fov}, \code{cellID} (or \code{cell_ID}) and \code{celltype_col}.}

\item{celltype_col}{Column name in \code{celltype_df} storing cell types.}

\item{celltype_palette}{Named vector mapping cell types to colors.}

\item{...}{Other parameters controlling zoom, scales, and point sizes.}
}
\value{
A list with two ggplot objects: \code{$full} and \code{$zoom}.
}
\description{
Plot CosMx cell polygons and gene transcripts with full + zoom panels
}
